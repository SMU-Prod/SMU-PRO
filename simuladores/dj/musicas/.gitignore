# Áudio do banco SMU NÃO entra no git (57+ MB de MP3).
# Estes arquivos são mídia, não código — o lugar deles é a hospedagem
# (Cloudflare R2 / Supabase Storage), apontada por CATALOGO_SMU.base
# em smu-dj-studio-real.html.
# Esta pasta existe para rodar/testar localmente.
*
!.gitignore
